export enum LoyaltyJoinMethodType {
  inviteOnly = 'invite_only',
  signUp = 'sign_up',
  amount = 'amount',
  transactionAmount = 'transaction_amount',
  pointsThreshold = 'points_threshold',
  points = 'points'
}
