import { DynamicGraphicDirective } from './dynamic-graphic.directive';

describe('DynamicGraphicDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicGraphicDirective();
    expect(directive).toBeTruthy();
  });
});
