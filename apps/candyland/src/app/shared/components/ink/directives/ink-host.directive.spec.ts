import { InkHostDirective } from './ink-host.directive';

describe('InkHostDirective', () => {
  it('should create an instance', () => {
    const directive = new InkHostDirective();
    expect(directive).toBeTruthy();
  });
});
