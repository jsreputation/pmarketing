import { InkListenerDirective } from './ink-listener.directive';

describe('InkListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new InkListenerDirective();
    expect(directive).toBeTruthy();
  });
});
