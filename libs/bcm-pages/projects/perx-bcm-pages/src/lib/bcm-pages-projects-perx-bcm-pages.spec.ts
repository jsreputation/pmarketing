import { bcmPagesProjectsPerxBcmPages } from './bcm-pages-projects-perx-bcm-pages';

describe('bcmPagesProjectsPerxBcmPages', () => {
  it('should work', () => {
    expect(bcmPagesProjectsPerxBcmPages()).toEqual(
      'bcm-pages-projects-perx-bcm-pages'
    );
  });
});
