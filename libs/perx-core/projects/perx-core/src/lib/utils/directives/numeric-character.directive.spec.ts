import { NumericCharacterDirective } from './numeric-character.directive';

describe('NumericCharacterDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericCharacterDirective();
    expect(directive).toBeTruthy();
  });
});
