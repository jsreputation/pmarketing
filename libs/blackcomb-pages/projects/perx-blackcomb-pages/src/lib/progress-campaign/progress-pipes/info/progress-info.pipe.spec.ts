import { ProgressInfoPipe } from './progress-info.pipe';

describe('ProgressInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ProgressInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
